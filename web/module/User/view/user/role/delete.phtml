<?= $this->partial('partial_header.phtml'); ?>

<main class="flex-shrink-0">
    <div class="container mt-5">
        <h2>Delete Role</h2>

        <?php
        // Display confirmation message
        echo '<p>Are you sure you want to delete the following role?</p>';
        echo '<p><strong>Name:</strong> ' . $this->escapeHtml($role->getName()) . '</p>';
        ?>

        <form method="post">
            <div class="mb-3">
                <input type="submit" name="confirmation" value="Yes" class="btn btn-danger">
                <a href="<?= $this->url('role-index'); ?>" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
</main>